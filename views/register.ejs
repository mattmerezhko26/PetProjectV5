<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.3/dist/tailwind.min.css" rel="stylesheet"/>
    <link rel="stylesheet" href="/css/main.css" />
    
  </head>
  <body tabindex="-1">
    <%- include('partials/navbar', { page: '' }) %>
    <!-- Centered Container -->
    <div class="flex items-center justify-center min-h-screen">
      <div class="w-full max-w-md bg-gray-800 p-6 rounded-lg shadow-lg">
        <% if (!successMessage) { %>
          <!-- Register Form -->
          <h2 class="text-3xl font-bold text-center text-gray-200 mb-6">
            Register
          </h2>
          <form method="POST" action="/register">
            <!-- User Name -->
            <div class="mb-4">
              <label
                for="userName"
                class="block text-gray-400 text-sm font-medium mb-1"
                >User Name</label
              >
              <input
                type="text"
                name="userName"
                id="userName"
                placeholder="User Name"
                value="<%= userName || '' %>"
                required
                class="w-full p-2 border border-gray-600 bg-gray-700 text-gray-200 rounded focus:ring focus:ring-green-500"
              />
            </div>

            <!-- Password -->
            <div class="mb-4">
              <label
                for="password"
                class="block text-gray-400 text-sm font-medium mb-1"
                >Password</label
              >
              <input
                type="password"
                name="password"
                id="password"
                placeholder="Password"
                required
                class="w-full p-2 border border-gray-600 bg-gray-700 text-gray-200 rounded focus:ring focus:ring-green-500"
              />
            </div>

            <!-- Confirm Password -->
            <div class="mb-4">
              <label
                for="password2"
                class="block text-gray-400 text-sm font-medium mb-1"
                >Confirm Password</label
              >
              <input
                type="password"
                name="password2"
                id="password2"
                placeholder="Confirm Password"
                required
                class="w-full p-2 border border-gray-600 bg-gray-700 text-gray-200 rounded focus:ring focus:ring-green-500"
              />
            </div>

            <!-- Email -->
            <div class="mb-6">
              <label
                for="email"
                class="block text-gray-400 text-sm font-medium mb-1"
                >Email Address</label
              >
              <input
                type="email"
                name="email"
                id="email"
                placeholder="Email Address"
                required
                class="w-full p-2 border border-gray-600 bg-gray-700 text-gray-200 rounded focus:ring focus:ring-green-500"
              />
            </div>

            <!-- Submit Button -->
            <button
              type="submit"
              class="w-full p-2 bg-green-500 text-white font-semibold rounded hover:bg-green-600 transition"
            >
              Register
            </button>
          </form>

          <!-- Error Message Alert -->
          <% if (errorMessage) { %>
          <div class="alert alert-error mt-4">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="stroke-current shrink-0 h-6 w-6"
              fill="none"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"
              />
            </svg>
            <span><%= errorMessage %></span>
          </div>
          <% } %>
        <% } else { %>
          <!-- Success Message -->
          <div class="alert mb-4">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="stroke-current shrink-0 h-6 w-6"
              fill="none"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
              />
            </svg>
            <span><%= successMessage %></span>
          </div>
          <!-- Proceed to Login Button -->
          <div class="text-center">
            <a href="/login" class="btn btn-success">Proceed to Log in</a>
          </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
