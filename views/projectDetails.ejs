<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Climate Solutions</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body tabindex="-1">
  <%- include('partials/navbar', { page: '/solutions/projects' }) %>

  <br />

  <div class="container mx-auto">

    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold"><%= project.title %></h1> 
          <p class="py-6">Below, you will find detailed information about the project: <%= project.title %></p> 
        </div>
      </div>
    </div>
  
    <br />
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <img class="w-full rounded-lg" src="<%= project.feature_img_url %>" alt="<%= project.title %> Image" /> 
      </div>
      <div class="prose">
        <h2>Introduction</h2>
        <p><%= project.intro_short %></p>
  
        <h2>Impact</h2>
        <p><%= project.impacts %></p> <!-- Make sure you're using 'impacts' instead of 'impact' -->
  
        <h2>Sector</h2>
        <p><%= project.Sector.sector_name %></p>
  
        <h2>Learn More</h2>
        <a href="<%= project.original_source_url %>" class="btn btn-primary">Find More Information</a> 
      </div>
    </div>
  
    <br />
    <!-- Edit Button -->
    <a href="/solutions/editProject/<%= project.id %>" class="btn btn-success mt-4">Edit Project</a>
  
    <br />
    
  
    <a href="#" onclick="history.back(); return false;" class="btn btn-secondary">Back</a>
  
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", () => {

      fetch('https://dummyjson.com/quotes/random')
        .then(response => response.json())
        .then(data => {
   
          document.getElementById('quote').textContent = `"${data.quote}"`;
          document.getElementById('author').textContent = `- ${data.author}`;
        })
        .catch(error => console.error('Error fetching quote:', error));
    });
  </script>

</body>

</html>
